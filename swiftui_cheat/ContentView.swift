
import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Hello,wirld")
            .padding(.top, 100)
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
